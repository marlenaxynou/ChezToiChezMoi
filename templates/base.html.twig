<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ChezToiChezMoi - Réservation d'hôtels{% endblock %}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link rel="icon" href="{{ asset('images/logo.png') }}">
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    {% block stylesheets %}{% endblock %}
</head>
<body class="{% block body_class %}{% endblock %}">
    {% include 'fragments/navigation.html.twig' %}

    {# Notifications Flash #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
        <div class="flash-message flash-{{ label }}" onclick="this.remove()">
            <i class="fas fa-{{ label == 'success' ? 'check-circle' : 'exclamation-circle' }}"></i>
            <span>{{ message }}</span>
            <i class="fas fa-times flash-close"></i>
        </div>
        {% endfor %}
    {% endfor %}

    <div class="main-content">
        {% block body %}{% endblock %}
    </div>
    
    {% include 'fragments/footer.html.twig' %}

<script src="{{ asset('js/flash.js') }}" defer></script>
</body>
</html>